#include <iostream>
#include <string>
#include "jogador.h"
#include "mesa.h"
#include "dado.h"
#include "colors.h"

using std::cin;
using std::cout;
using std::endl;
class Lobby
{
private:
    /* data */
public:
    Lobby(/* args */);
    ~Lobby();
    int runGame();
};
